# Source Code

Place product source code under this directory using a structure appropriate for the selected language and architecture.

Before adding code:

- Update `AGENTS.md` with real build, test, run, and formatting commands.
- Keep component boundaries consistent with `docs/ARCHITECTURE.md`.
- Do not place secrets or real production data here.
- Add a short README inside major components when their purpose or local workflow is not obvious.

Delete this placeholder file when the real source structure makes it unnecessary.
